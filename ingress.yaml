apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-iot-ingress
spec:
  rules:
  - host: hello-iot.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello-iot-service
            port:
              number: 80
